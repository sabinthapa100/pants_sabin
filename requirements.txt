# PyTorch is intentionally NOT pinned here.
#
# The correct wheel depends on your GPU, CUDA runtime, and platform, so install
# it first following https://pytorch.org/get-started/locally/ and only then run
# `pip install -r requirements.txt`.
#
# Verified working combination for this repository:
#   Python      3.11.15
#   PyTorch     2.13.0+cu126
#   MONAI       1.5.1
#   nnU-Net v2  2.8.1
#
# nnunetv2 and monai both declare a torch dependency, so an existing correct
# torch install is respected rather than replaced.

# Imported directly by this repository.
numpy
nibabel
matplotlib
pandas           # manifest.py only, to read the optional metadata.xlsx
openpyxl         # the .xlsx engine pandas needs; not imported by name

# scipy, scikit-image, SimpleITK and tqdm were listed here but are never
# imported by this project; nnunetv2 declares them and pip installs them.
# PyYAML was left over from the deleted configs/ directory.

# 3D SegResNet, transforms, and sliding-window inference.
monai==1.5.1

# Independent nnU-Net v2 baseline.
nnunetv2==2.8.1

pytest==9.1.1
